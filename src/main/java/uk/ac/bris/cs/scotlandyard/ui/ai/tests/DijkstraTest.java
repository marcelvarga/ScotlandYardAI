package uk.ac.bris.cs.scotlandyard.ui.ai.tests;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;
import uk.ac.bris.cs.scotlandyard.ui.ai.Dijkstra;
import uk.ac.bris.cs.scotlandyard.model.Board.GameState;
import static uk.ac.bris.cs.scotlandyard.model.ScotlandYard.*;

class DijkstraTest {

    //Create a board to test it on - no idea how to do this!
    //GameState state = gameStateFactory.build(standard24RoundSetup(), blackPlayer(), bluePlayer);

    @org.junit.jupiter.api.Test
    public void testDijkstraCreation() {
        //Dijkstra d = new Dijkstra(board, mrXLocation, detectiveLocations);

    }
}